interface Props {
  enabled: boolean;
  isPending: boolean;
  isError: boolean;
  hasData: boolean;
}

const useChartStatus = ({ enabled, isPending, isError, hasData }: Props) => {
  return null;
};

export default useChartStatus;
